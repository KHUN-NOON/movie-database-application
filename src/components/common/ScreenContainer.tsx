import { styled, Box } from "@mui/material"

const ScreenContainer = styled(Box)({
    marginTop: 15,
    paddingInline: 20,
    paddingBottom: 20,
    width: '100%'
})

export default ScreenContainer